import { makeStyles } from '@material-ui/core/styles'

const useStyles = makeStyles((theme) => ({
  container: {
    border: '1px solid',
    borderColor: theme.palette.icons.wildSand,
  },
}))

export default useStyles
